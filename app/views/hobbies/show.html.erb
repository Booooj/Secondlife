<div class="pd"> 
投稿者
<p>  <%= @hobby.user.name %>さん</p>
    投稿内容
    <p><img src="<%= "/post_images/#{@hobby.image}" %>" width="200" height="200"></p>
      <p> <%= @hobby.body %></p>
      <% if @hobby.user_id == @current_user.id %>
      <%= link_to("削除", "/hobbies/#{@hobby.id}/destroy", {method: "hobby"}) %>
      <% end %>
</div>
