<div class="text">
      <h1>アカウント情報</h1>
      <h2><%= @user.name %></h2>
      <% if @user.id == @current_user.id %>
      <p><%= @user.email %></p>
        <%= link_to("アカウントを編集する", "/users/#{@user.id}/edit") %>
      <% end %>
      